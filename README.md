# bank rest API

Это тестовое задание на REST API-сервис для управления банковскими картами, реализованный на Spring Boot с использованием Java 17+, Spring Security, Spring Data JPA, PostgreSQL, Liquibase, Docker, JWT, Swagger (OpenAPI).

---

## 🚀 Быстрый старт с Docker Compose

Для запуска проекта убедитесь, что у вас установлен Docker и Docker Compose.

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Mitya4395228/bank_rest.git
   cd bank_rest
   ```

2. Соберите и запустите контейнеры:
   ```bash
   docker-compose up --build
   ```

3. Приложение будет доступно по адресу:
   ```
   http://localhost:8080
   ```

---

## 📦 Инициализация базы данных

При первом запуске база данных инициализируется, скриптами Liquibase, в каталоге `src/main/resources/db/migration`, в базу будут добавлены тестовые пользователи и тестовые карты для каждого пользователя с начальным балансом 1000:

| Username      | Password | Роль   |
|---------------|----------|--------|
| `admin-test`  | `123456` | ADMIN  |
| `user-test`   | `123456` | USER   |

---

## 🔐 Аутентификация

Для получения JWT-токена используйте endpoint:

```
POST /login
Content-Type: application/json
```

Пример тела запроса:
```json
{
  "username": "admin-test",
  "password": "123456"
}
```

При успешной авторизации в ответе будет JWT-токен. Его нужно передавать в заголовке всех защищённых запросов:

```
Authorization: Bearer <ваш токен>
```

---

## 📚 Документация API

После запуска API-документация будет доступна по адресу:

```
http://localhost:8080/swagger-ui/index.html
```

---

## 🐘 Конфигурация PostgreSQL

- Хост: `localhost`
- Порт: `5432`
- Пользователь: `user_manager_service`
- Пароль: `123456`
- База данных: `user_service_db`

---

## 📁 Структура проекта

```
.
├── src/
├── docker-compose.yml
├── Dockerfile
├── init/                # SQL-инициализация базы
├── README.md
```

---

## 🛠️ Стек технологий

- Java 17+
- Spring Boot
- Spring Security + JWT
- PostgreSQL
- Liquibase
- Docker / Docker Compose
- Swagger / OpenAPI

---

## 📬 Обратная связь

Вопросы и предложения — добро пожаловать в Issues или Pull Requests.
